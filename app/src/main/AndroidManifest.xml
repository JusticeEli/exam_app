<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.justice.examapp">

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentsMarksActivity"></activity>
        <activity android:name=".TestCompleteActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".StudentFirstPageActivity" />
        <activity android:name=".QuestionAddActivity" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".TeacherFirstPageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>